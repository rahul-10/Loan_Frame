import React, { Component } from 'react';
import { AppRegistry } from 'react-native';

import Navigator from './src/com/loan_frame/Navigator';

AppRegistry.registerComponent('demo', () => Navigator);
